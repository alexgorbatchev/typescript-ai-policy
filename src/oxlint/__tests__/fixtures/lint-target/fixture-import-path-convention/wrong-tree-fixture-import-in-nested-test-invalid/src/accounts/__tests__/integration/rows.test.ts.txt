import { expect, test } from "bun:test";
import { fixture_userAccountRows } from "../../other-feature/__tests__/fixtures";

test("uses fixture", () => {
  expect(fixture_userAccountRows).toEqual([]);
});
