import { describe, expect, it } from "bun:test";
import { RouteShell } from "../layouts/RouteShell";

describe("RouteShell", () => {
  it("creates a wrapper harness", () => {
    const harness = <div className="test-shell"><RouteShell /></div>;

    expect(harness).toBeDefined();
  });
});
