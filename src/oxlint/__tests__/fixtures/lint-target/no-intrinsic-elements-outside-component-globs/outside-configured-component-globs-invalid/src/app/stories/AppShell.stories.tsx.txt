import type { Meta, StoryObj } from "@storybook/react";
import { AppShell } from "../AppShell";

const meta: Meta<typeof AppShell> = {
  component: AppShell,
  decorators: [(Story) => <div className="story-shell"><Story /></div>],
  title: "outside-configured-component-globs-invalid/app/AppShell",
};

export default meta;

type Story = StoryObj<typeof meta>;

const Default: Story = {
  play: async () => {},
};

export { Default as AppShell };
