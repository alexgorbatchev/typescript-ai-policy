import type { Meta, StoryObj } from "@storybook/react";
import { Missing } from "../Missing";

const meta: Meta<typeof Missing> = {
  component: Missing,
  title: "accounts/components/Missing",
};

export default meta;

type Story = StoryObj<typeof meta>;

const Default: Story = {
  play: async () => {
    console.log("play");
  },
};

export { Default as Missing };
